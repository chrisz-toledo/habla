"""
ARCHIVO OBSOLETO — No usar.

El backend Go real está en go_transpiler.py (clase GoTranspiler).
Este archivo es un stub v0.1 que quedó huérfano y no está conectado al registry.

Ver: src/hado/backends/base.py → "go" → "hado.backends.go_transpiler.GoTranspiler"
"""

raise ImportError(
    "go_backend.py está obsoleto. "
    "Usa: from hado.backends.go_transpiler import GoTranspiler"
)
